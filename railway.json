{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "cd /app && php -S 0.0.0.0:${PORT:-8080} -t public /app/router.php",
    "healthcheckPath": "/",
    "restartPolicyType": "always"
  }
}
