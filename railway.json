{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "touch /app/database/database.sqlite && php artisan migrate --force && php -S 0.0.0.0:${PORT:-8080} -t public /app/router.php",
    "healthcheckPath": "/",
    "restartPolicyType": "always"
  }
}
