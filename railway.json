{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "php -S 0.0.0.0:${PORT:-8080} -t public router.php",
    "healthcheckPath": "/",
    "restartPolicyType": "always"
  }
}
